{
  "name": "RNA-seq Practical",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "postCreateCommand": "fastqc --version && salmon --version",
  "remoteUser": "root"
}
